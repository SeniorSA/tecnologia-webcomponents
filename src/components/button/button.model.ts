export enum TecButtonSize {
  tiny = 'tiny',
  small = 'small',
  medium = 'medium',
  large = 'large',
  giant = 'giant',
}

export enum TecButtonMode {
  rounded = 'rounded',
  square = 'square',
  radius = 'radius',
}

export enum TecButtonColor {
  solid = 'solid',
  gradient = 'gradient',
  outline = 'outline',
  basic = 'basic',
}

export enum TecButtonIconMode {
  left = 'left',
  right = 'right',
}
